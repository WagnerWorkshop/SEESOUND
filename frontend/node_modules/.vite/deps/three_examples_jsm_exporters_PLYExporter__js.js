import { Kr as Matrix3, at as Color, bs as Vector3, co as SRGBColorSpace, st as ColorManagement } from "./three.module-DMWBM_3t.js";
//#region node_modules/.pnpm/three@0.171.0/node_modules/three/examples/jsm/exporters/PLYExporter.js
/**
* https://github.com/gkjohnson/ply-exporter-js
*
* Usage:
*  const exporter = new PLYExporter();
*
*  // second argument is a list of options
*  exporter.parse(mesh, data => console.log(data), { binary: true, excludeAttributes: [ 'color' ], littleEndian: true });
*
* Format Definition:
* http://paulbourke.net/dataformats/ply/
*/
var PLYExporter = class {
	parse(object, onDone, options = {}) {
		function traverseMeshes(cb) {
			object.traverse(function(child) {
				if (child.isMesh === true || child.isPoints) {
					const mesh = child;
					const geometry = mesh.geometry;
					if (geometry.hasAttribute("position") === true) cb(mesh, geometry);
				}
			});
		}
		options = Object.assign({
			binary: false,
			excludeAttributes: [],
			littleEndian: false
		}, options);
		const excludeAttributes = options.excludeAttributes;
		let includeIndices = true;
		let includeNormals = false;
		let includeColors = false;
		let includeUVs = false;
		let vertexCount = 0;
		let faceCount = 0;
		object.traverse(function(child) {
			if (child.isMesh === true) {
				const geometry = child.geometry;
				const vertices = geometry.getAttribute("position");
				const normals = geometry.getAttribute("normal");
				const uvs = geometry.getAttribute("uv");
				const colors = geometry.getAttribute("color");
				const indices = geometry.getIndex();
				if (vertices === void 0) return;
				vertexCount += vertices.count;
				faceCount += indices ? indices.count / 3 : vertices.count / 3;
				if (normals !== void 0) includeNormals = true;
				if (uvs !== void 0) includeUVs = true;
				if (colors !== void 0) includeColors = true;
			} else if (child.isPoints) {
				const geometry = child.geometry;
				const vertices = geometry.getAttribute("position");
				const normals = geometry.getAttribute("normal");
				const colors = geometry.getAttribute("color");
				vertexCount += vertices.count;
				if (normals !== void 0) includeNormals = true;
				if (colors !== void 0) includeColors = true;
				includeIndices = false;
			}
		});
		const tempColor = new Color();
		includeIndices = includeIndices && excludeAttributes.indexOf("index") === -1;
		includeNormals = includeNormals && excludeAttributes.indexOf("normal") === -1;
		includeColors = includeColors && excludeAttributes.indexOf("color") === -1;
		includeUVs = includeUVs && excludeAttributes.indexOf("uv") === -1;
		if (includeIndices && faceCount !== Math.floor(faceCount)) {
			console.error("PLYExporter: Failed to generate a valid PLY file with triangle indices because the number of indices is not divisible by 3.");
			return null;
		}
		const indexByteCount = 4;
		let header = `ply
format ${options.binary ? options.littleEndian ? "binary_little_endian" : "binary_big_endian" : "ascii"} 1.0\nelement vertex ${vertexCount}\nproperty float x
property float y
property float z
`;
		if (includeNormals === true) header += "property float nx\nproperty float ny\nproperty float nz\n";
		if (includeUVs === true) header += "property float s\nproperty float t\n";
		if (includeColors === true) header += "property uchar red\nproperty uchar green\nproperty uchar blue\n";
		if (includeIndices === true) header += `element face ${faceCount}\nproperty list uchar int vertex_index
`;
		header += "end_header\n";
		const vertex = new Vector3();
		const normalMatrixWorld = new Matrix3();
		let result = null;
		if (options.binary === true) {
			const headerBin = new TextEncoder().encode(header);
			const vertexListLength = vertexCount * (12 + (includeNormals ? 12 : 0) + (includeColors ? 3 : 0) + (includeUVs ? 8 : 0));
			const faceListLength = includeIndices ? faceCount * 13 : 0;
			const output = new DataView(new ArrayBuffer(headerBin.length + vertexListLength + faceListLength));
			new Uint8Array(output.buffer).set(headerBin, 0);
			let vOffset = headerBin.length;
			let fOffset = headerBin.length + vertexListLength;
			let writtenVertices = 0;
			traverseMeshes(function(mesh, geometry) {
				const vertices = geometry.getAttribute("position");
				const normals = geometry.getAttribute("normal");
				const uvs = geometry.getAttribute("uv");
				const colors = geometry.getAttribute("color");
				const indices = geometry.getIndex();
				normalMatrixWorld.getNormalMatrix(mesh.matrixWorld);
				for (let i = 0, l = vertices.count; i < l; i++) {
					vertex.fromBufferAttribute(vertices, i);
					vertex.applyMatrix4(mesh.matrixWorld);
					output.setFloat32(vOffset, vertex.x, options.littleEndian);
					vOffset += 4;
					output.setFloat32(vOffset, vertex.y, options.littleEndian);
					vOffset += 4;
					output.setFloat32(vOffset, vertex.z, options.littleEndian);
					vOffset += 4;
					if (includeNormals === true) if (normals != null) {
						vertex.fromBufferAttribute(normals, i);
						vertex.applyMatrix3(normalMatrixWorld).normalize();
						output.setFloat32(vOffset, vertex.x, options.littleEndian);
						vOffset += 4;
						output.setFloat32(vOffset, vertex.y, options.littleEndian);
						vOffset += 4;
						output.setFloat32(vOffset, vertex.z, options.littleEndian);
						vOffset += 4;
					} else {
						output.setFloat32(vOffset, 0, options.littleEndian);
						vOffset += 4;
						output.setFloat32(vOffset, 0, options.littleEndian);
						vOffset += 4;
						output.setFloat32(vOffset, 0, options.littleEndian);
						vOffset += 4;
					}
					if (includeUVs === true) if (uvs != null) {
						output.setFloat32(vOffset, uvs.getX(i), options.littleEndian);
						vOffset += 4;
						output.setFloat32(vOffset, uvs.getY(i), options.littleEndian);
						vOffset += 4;
					} else {
						output.setFloat32(vOffset, 0, options.littleEndian);
						vOffset += 4;
						output.setFloat32(vOffset, 0, options.littleEndian);
						vOffset += 4;
					}
					if (includeColors === true) if (colors != null) {
						tempColor.fromBufferAttribute(colors, i);
						ColorManagement.fromWorkingColorSpace(tempColor, SRGBColorSpace);
						output.setUint8(vOffset, Math.floor(tempColor.r * 255));
						vOffset += 1;
						output.setUint8(vOffset, Math.floor(tempColor.g * 255));
						vOffset += 1;
						output.setUint8(vOffset, Math.floor(tempColor.b * 255));
						vOffset += 1;
					} else {
						output.setUint8(vOffset, 255);
						vOffset += 1;
						output.setUint8(vOffset, 255);
						vOffset += 1;
						output.setUint8(vOffset, 255);
						vOffset += 1;
					}
				}
				if (includeIndices === true) if (indices !== null) for (let i = 0, l = indices.count; i < l; i += 3) {
					output.setUint8(fOffset, 3);
					fOffset += 1;
					output.setUint32(fOffset, indices.getX(i + 0) + writtenVertices, options.littleEndian);
					fOffset += indexByteCount;
					output.setUint32(fOffset, indices.getX(i + 1) + writtenVertices, options.littleEndian);
					fOffset += indexByteCount;
					output.setUint32(fOffset, indices.getX(i + 2) + writtenVertices, options.littleEndian);
					fOffset += indexByteCount;
				}
				else for (let i = 0, l = vertices.count; i < l; i += 3) {
					output.setUint8(fOffset, 3);
					fOffset += 1;
					output.setUint32(fOffset, writtenVertices + i, options.littleEndian);
					fOffset += indexByteCount;
					output.setUint32(fOffset, writtenVertices + i + 1, options.littleEndian);
					fOffset += indexByteCount;
					output.setUint32(fOffset, writtenVertices + i + 2, options.littleEndian);
					fOffset += indexByteCount;
				}
				writtenVertices += vertices.count;
			});
			result = output.buffer;
		} else {
			let writtenVertices = 0;
			let vertexList = "";
			let faceList = "";
			traverseMeshes(function(mesh, geometry) {
				const vertices = geometry.getAttribute("position");
				const normals = geometry.getAttribute("normal");
				const uvs = geometry.getAttribute("uv");
				const colors = geometry.getAttribute("color");
				const indices = geometry.getIndex();
				normalMatrixWorld.getNormalMatrix(mesh.matrixWorld);
				for (let i = 0, l = vertices.count; i < l; i++) {
					vertex.fromBufferAttribute(vertices, i);
					vertex.applyMatrix4(mesh.matrixWorld);
					let line = vertex.x + " " + vertex.y + " " + vertex.z;
					if (includeNormals === true) if (normals != null) {
						vertex.fromBufferAttribute(normals, i);
						vertex.applyMatrix3(normalMatrixWorld).normalize();
						line += " " + vertex.x + " " + vertex.y + " " + vertex.z;
					} else line += " 0 0 0";
					if (includeUVs === true) if (uvs != null) line += " " + uvs.getX(i) + " " + uvs.getY(i);
					else line += " 0 0";
					if (includeColors === true) if (colors != null) {
						tempColor.fromBufferAttribute(colors, i);
						ColorManagement.fromWorkingColorSpace(tempColor, SRGBColorSpace);
						line += " " + Math.floor(tempColor.r * 255) + " " + Math.floor(tempColor.g * 255) + " " + Math.floor(tempColor.b * 255);
					} else line += " 255 255 255";
					vertexList += line + "\n";
				}
				if (includeIndices === true) {
					if (indices !== null) for (let i = 0, l = indices.count; i < l; i += 3) {
						faceList += `3 ${indices.getX(i + 0) + writtenVertices}`;
						faceList += ` ${indices.getX(i + 1) + writtenVertices}`;
						faceList += ` ${indices.getX(i + 2) + writtenVertices}\n`;
					}
					else for (let i = 0, l = vertices.count; i < l; i += 3) faceList += `3 ${writtenVertices + i} ${writtenVertices + i + 1} ${writtenVertices + i + 2}\n`;
					faceCount += indices ? indices.count / 3 : vertices.count / 3;
				}
				writtenVertices += vertices.count;
			});
			result = `${header}${vertexList}${includeIndices ? `${faceList}\n` : "\n"}`;
		}
		if (typeof onDone === "function") requestAnimationFrame(() => onDone(result));
		return result;
	}
};
//#endregion
export { PLYExporter };
